<!--
提交信息请用约定式提交前缀，release-please 据此自动生成版本与 Release：
  feat: …    新功能（触发 minor）
  fix: …     缺陷修复（触发 patch）
  chore: / docs: / refactor: / test: / ci: …   不触发版本
破坏性变更在正文加 `BREAKING CHANGE:` 或前缀用 `feat!:`。
-->

## 变更内容 / What changed



## 关联 Issue / Related issue

<!-- 例：closes #12 -->

## 自测 / Self-check

- [ ] `cd backend && uv run pytest -q` 通过 / passes
- [ ] `cd frontend && npm run build` 通过 / passes
- [ ] 手动验证了受影响的功能 / manually verified the affected feature

## 运行环境 / Environment

<!-- 仅在涉及运行时行为时填写 / only if runtime behaviour is involved -->

- OS（`winver`）：
- Python（`python -V`）：
- Node（`node -v`）：
- 本项目版本（标题旁徽标 / `version.txt`）：
